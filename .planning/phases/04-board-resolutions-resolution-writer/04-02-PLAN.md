---
phase: 04-board-resolutions-resolution-writer
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/app/tools/resolutions.py
  - backend/app/tools/__init__.py
  - backend/app/main.py
autonomous: true
requirements: [BUILT-03]

must_haves:
  truths:
    - "Resolution Writer agent tools are registered in TOOL_REGISTRY"
    - "create_resolution tool creates a decision with type=resolution via REST API"
    - "draft_resolution_document tool creates an HTML document via documents API and links to decision"
    - "list_resolutions tool returns resolutions via the /api/resolutions endpoint"
    - "get_resolution tool returns resolution detail via /api/resolutions/{id}"
    - "Resolution Writer seed data has production system prompt (no placeholder text)"
    - "Resolution Writer seed data allowed_tool_names match actual registered tool names"
  artifacts:
    - path: "backend/app/tools/resolutions.py"
      provides: "Resolution Writer agent tool handlers"
      min_lines: 100
    - path: "backend/app/tools/__init__.py"
      provides: "Auto-import of resolutions tool module"
      contains: "from app.tools import resolutions"
    - path: "backend/app/main.py"
      provides: "Updated seed data for Resolution Writer agent"
      contains: "_RESOLUTION_WRITER_SYSTEM_PROMPT"
  key_links:
    - from: "backend/app/tools/resolutions.py"
      to: "/api/resolutions"
      via: "httpx.AsyncClient internal API calls"
      pattern: "/api/resolutions"
    - from: "backend/app/tools/resolutions.py"
      to: "/api/decisions"
      via: "httpx.AsyncClient for decision creation"
      pattern: "/api/decisions"
    - from: "backend/app/tools/__init__.py"
      to: "backend/app/tools/resolutions.py"
      via: "import at module bottom"
      pattern: "from app.tools import resolutions"
---

<objective>
Implement the Resolution Writer agent: four tool handlers (create_resolution, draft_resolution_document, list_resolutions, get_resolution), a production system prompt, and updated seed data in main.py.

Purpose: The Resolution Writer agent enables board users to draft formal resolution documents from a brief description, creating both the decision record and an optional HTML document linked to it.
Output: Fully registered tools, production prompt, updated seed config.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-board-resolutions-resolution-writer/04-RESEARCH.md
@.planning/phases/04-board-resolutions-resolution-writer/04-01-SUMMARY.md

@backend/app/tools/__init__.py
@backend/app/tools/transcripts.py
@backend/app/tools/meetings.py
@backend/app/main.py

<interfaces>
From backend/app/tools/__init__.py:
```python
@dataclass
class ToolDefinition:
    name: str
    description: str
    parameters_schema: dict
    handler: Callable  # async function(params: dict, user_context: dict) -> str
    category: str

def register_tool(tool: ToolDefinition) -> None: ...
```

From backend/app/api/resolutions.py (created in 04-01):
```
GET  /api/resolutions                    -> list resolutions
GET  /api/resolutions/{id}               -> resolution detail
POST /api/resolutions/{id}/sign          -> sign resolution
GET  /api/resolutions/{id}/signatures    -> signature status
```

From backend/app/api/decisions.py:
```
POST /api/decisions                      -> create decision (accepts type=resolution)
```

From backend/app/api/documents.py:
```
POST /api/documents                      -> upload/create document
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolution Writer agent tools</name>
  <files>backend/app/tools/resolutions.py, backend/app/tools/__init__.py</files>
  <action>
1. Create `backend/app/tools/resolutions.py` following the EXACT pattern from `tools/transcripts.py` (same imports, same `_get_base_url()` helper, same error handling with `response.status_code >= 400`).

Use `from __future__ import annotations` at top for Python 3.9 compat.

Implement four tools:

**create_resolution**: POST /api/decisions with body {title, description, type: "resolution", resolution_number (optional), meeting_id (optional)}. Parameters schema: title (required string), description (required string), resolution_number (optional string, e.g. "2026-001"), meeting_id (optional integer).

**draft_resolution_document**: Creates a formal HTML resolution document. Takes resolution_id (required int), title (required string), html_content (required string). POST to /api/documents with {title, type: "resolution", content: html_content} then PATCH /api/decisions/{resolution_id} to set document_id to the created document's ID. If document creation fails, return the error. If linking fails, return partial success with document_id.

**list_resolutions**: GET /api/resolutions. Optional status parameter. Returns the JSON response.

**get_resolution**: GET /api/resolutions/{resolution_id}. Takes resolution_id (required int). Returns resolution detail.

Each tool: register_tool(ToolDefinition(...)) at module level with category="resolutions".

2. In `backend/app/tools/__init__.py`, add at the bottom:
```python
from app.tools import resolutions  # noqa: E402, F401
```
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/ivy/tmg-board/backend && python -c "from app.tools import TOOL_REGISTRY; tools = [t for t in TOOL_REGISTRY if 'resolution' in t]; print(f'Resolution tools: {tools}'); assert len(tools) >= 3, f'Expected >=3 resolution tools, got {len(tools)}'"</automated>
  </verify>
  <done>Four resolution tools registered in TOOL_REGISTRY: create_resolution, draft_resolution_document, list_resolutions, get_resolution</done>
</task>

<task type="auto">
  <name>Task 2: Resolution Writer production system prompt and seed data update</name>
  <files>backend/app/main.py</files>
  <action>
1. In `backend/app/main.py`, add a `_RESOLUTION_WRITER_SYSTEM_PROMPT` constant (after `_MINUTES_GENERATOR_SYSTEM_PROMPT`). The prompt should instruct the agent:

```
You are the Resolution Writer Agent for The Many Group board governance platform.

Your job is to draft formal board resolution documents from brief descriptions.

Follow this workflow:
1. When given a resolution description, create a decision of type=resolution using the create_resolution tool. Include a clear title and formal description text.
2. If asked to produce a formal document, draft an HTML resolution document using draft_resolution_document. The HTML should follow formal resolution format:
   - WHEREAS clauses stating the context and rationale
   - RESOLVED clauses stating the actions or decisions
   - Signature block placeholder at the bottom
3. Use list_resolutions and get_resolution to check existing resolutions when needed.

Resolution formatting rules:
- Title: "Resolution [Number]: [Subject]" format
- WHEREAS clauses: Each starts with "WHEREAS," on its own line
- RESOLVED clauses: Each starts with "NOW, THEREFORE, BE IT RESOLVED" or "BE IT FURTHER RESOLVED"
- Use formal, legal-style language appropriate for board governance
- Keep resolutions concise but comprehensive
- Always call create_resolution first, then optionally draft_resolution_document for formal HTML

HTML document formatting:
- Use <h1> for the resolution title
- Use <p> for WHEREAS and RESOLVED clauses with <strong> for the keywords
- Use <table> for signature blocks
- Include date, resolution number, and organization name in the header
- Keep styling minimal (will be rendered with print styles)
```

2. In the `_seed_agents()` function, add a new upgrade block (after the Minutes Generator upgrade block) that detects the Resolution Writer placeholder prompt "[Detailed prompt to be added in Phase 04]" and updates it:

```python
# Update existing Resolution Writer if it still has the Phase 01 placeholder
resolution_agent = db.query(AgentConfig).filter(
    AgentConfig.slug == "resolution-writer"
).first()
if resolution_agent and "[Detailed prompt to be added in Phase 04]" in (
    resolution_agent.system_prompt or ""
):
    resolution_agent.system_prompt = _RESOLUTION_WRITER_SYSTEM_PROMPT
    resolution_agent.allowed_tool_names = [
        "create_resolution",
        "draft_resolution_document",
        "list_resolutions",
        "get_resolution",
    ]
    db.commit()
```

3. Also update the initial seed data for Resolution Writer (in the seed_agents list at the top of the function) to use the production prompt and correct tool names, so fresh installs get the right config.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/ivy/tmg-board/backend && python -c "from app.main import _RESOLUTION_WRITER_SYSTEM_PROMPT; assert 'WHEREAS' in _RESOLUTION_WRITER_SYSTEM_PROMPT; print('Prompt OK')"</automated>
  </verify>
  <done>Resolution Writer has production system prompt with WHEREAS/RESOLVED format instructions. Seed data upgrade block detects placeholder and updates. Fresh installs get correct config.</done>
</task>

</tasks>

<verification>
- `python -c "from app.tools import TOOL_REGISTRY; print([k for k in TOOL_REGISTRY if 'resolution' in k])"` shows 4 tools
- `python -c "from app.main import _RESOLUTION_WRITER_SYSTEM_PROMPT; print(len(_RESOLUTION_WRITER_SYSTEM_PROMPT))"` shows non-trivial prompt
- `cd backend && python -m pytest tests/ -v` passes (all existing + resolution tests from 04-01)
</verification>

<success_criteria>
Resolution Writer agent is fully configured with production prompt, four registered tools, and upgrade-safe seed data. Tools call the board REST API via httpx following established patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/04-board-resolutions-resolution-writer/04-02-SUMMARY.md`
</output>
