---
phase: 04-board-resolutions-resolution-writer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/decision.py
  - backend/app/models/__init__.py
  - backend/app/schemas/resolution.py
  - backend/migrations/versions/007_add_resolution_signatures.py
  - backend/app/api/resolutions.py
  - backend/app/main.py
  - backend/tests/test_resolutions.py
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]

must_haves:
  truths:
    - "GET /api/resolutions returns only decisions with type=resolution"
    - "POST /api/resolutions/{id}/sign records a digital signature with name, timestamp, and IP"
    - "GET /api/resolutions/{id}/signatures shows who signed and who has not"
    - "Signing a non-resolution decision returns 400"
    - "Signing a non-closed resolution returns 400"
    - "Duplicate signatures are rejected (UniqueConstraint)"
    - "Resolution auto-numbering generates YYYY-NNN format"
  artifacts:
    - path: "backend/app/models/decision.py"
      provides: "ResolutionSignature model with decision_id, member_id, signed_at, ip_address, signature_hash"
      contains: "class ResolutionSignature"
    - path: "backend/app/schemas/resolution.py"
      provides: "Pydantic schemas for resolution API requests/responses"
      exports: ["SignatureStatusResponse", "SignatureResponse"]
    - path: "backend/migrations/versions/007_add_resolution_signatures.py"
      provides: "Alembic migration creating resolution_signatures table"
      contains: "resolution_signatures"
    - path: "backend/app/api/resolutions.py"
      provides: "Resolutions API router (list, get, sign, signatures)"
      exports: ["router"]
    - path: "backend/tests/test_resolutions.py"
      provides: "Integration tests for resolution API"
      min_lines: 80
  key_links:
    - from: "backend/app/api/resolutions.py"
      to: "backend/app/models/decision.py"
      via: "ResolutionSignature and Decision model queries"
      pattern: "ResolutionSignature|Decision"
    - from: "backend/app/main.py"
      to: "backend/app/api/resolutions.py"
      via: "app.include_router(resolutions.router)"
      pattern: "resolutions\\.router"
---

<objective>
Create the backend infrastructure for board resolution signatures: the ResolutionSignature model, database migration, Pydantic schemas, resolution-specific API endpoints (list, get detail, sign, signature status), and integration tests.

Purpose: Board members need to digitally sign closed resolutions (name + timestamp + IP). The existing Decision model already supports type=resolution; this plan adds the signature layer and dedicated API.
Output: Working resolution API at /api/resolutions with sign/signatures endpoints, fully tested.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-board-resolutions-resolution-writer/04-RESEARCH.md

@backend/app/models/decision.py
@backend/app/models/__init__.py
@backend/app/api/decisions.py
@backend/app/api/auth.py
@backend/app/main.py
@backend/tests/conftest.py
@backend/migrations/versions/006_add_transcripts.py

<interfaces>
From backend/app/models/decision.py:
```python
class Decision(Base):
    __tablename__ = "decisions"
    id: Mapped[int]
    title: Mapped[str]
    type: Mapped[str]  # vote/consent/resolution
    status: Mapped[str]  # draft/open/closed
    resolution_number: Mapped[Optional[str]]
    document_id: Mapped[Optional[int]]
    created_by_id: Mapped[int]
    deleted_at: Mapped[Optional[datetime]]
    archived_at: Mapped[Optional[datetime]]
    votes: Mapped[List["Vote"]] = relationship(...)

class Vote(Base):
    __tablename__ = "votes"
    id: Mapped[int]
    decision_id: Mapped[int]
    member_id: Mapped[int]
    vote: Mapped[str]  # yes/no/abstain
```

From backend/app/api/auth.py:
```python
def require_member(...)  -> BoardMember  # Any board-level member
def require_chair(...)   -> BoardMember  # Chair or admin only
```

From backend/app/models/member.py:
```python
class BoardMember(Base):
    __tablename__ = "board_members"
    id: Mapped[int]
    email: Mapped[str]
    name: Mapped[str]
    role: Mapped[str]  # admin/chair/board/shareholder
    deleted_at: Mapped[Optional[datetime]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResolutionSignature model, migration, and schemas</name>
  <files>backend/app/models/decision.py, backend/app/models/__init__.py, backend/app/schemas/resolution.py, backend/migrations/versions/007_add_resolution_signatures.py</files>
  <action>
1. In `backend/app/models/decision.py`, add `ResolutionSignature` class AFTER the `Vote` class, following the exact Vote model pattern. Fields: `id` (int PK), `decision_id` (int FK to decisions.id with CASCADE ondelete), `member_id` (int FK to board_members.id), `signed_at` (DateTime, default=datetime.utcnow), `ip_address` (String(45), nullable=True), `signature_hash` (String(64), nullable=False). Add `__table_args__` with `UniqueConstraint("decision_id", "member_id", name="uq_resolution_member_signature")`. Add relationships: `decision` back to Decision, `member` to BoardMember.

2. In the `Decision` class, add a `signatures` relationship: `signatures: Mapped[List["ResolutionSignature"]] = relationship("ResolutionSignature", back_populates="decision", cascade="all, delete-orphan")`.

3. Add a standalone `generate_signature_hash()` function that takes `member_name: str, member_email: str, decision_id: int, signed_at: datetime` and returns `hashlib.sha256(f"{member_name}:{member_email}:{decision_id}:{signed_at.isoformat()}".encode()).hexdigest()`. Import hashlib at the top.

4. In `backend/app/models/__init__.py`, add `ResolutionSignature` to the import from decision.py and to `__all__`.

5. Create `backend/app/schemas/resolution.py` with Pydantic models:
   - `SignatureResponse` (status: str, signature_id: int, signed_at: str)
   - `SignatureStatusResponse` (resolution_id: int, signatures: list, signed_count: int, total_members: int)
   - `ResolutionListItem` extending Decision fields + signature_count: int, total_signers: int, resolution_number: Optional[str]

6. Create `backend/migrations/versions/007_add_resolution_signatures.py` following the exact pattern from 006_add_transcripts.py. The migration creates the `resolution_signatures` table with columns matching the model. Include the UniqueConstraint and ForeignKeys. Use `from __future__ import annotations` for Python 3.9 compat.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/ivy/tmg-board/backend && python -c "from app.models.decision import ResolutionSignature, generate_signature_hash; print('Model OK')"</automated>
  </verify>
  <done>ResolutionSignature model importable, generate_signature_hash function exists, migration file created, schemas defined</done>
</task>

<task type="auto">
  <name>Task 2: Resolutions API endpoints, router registration, and tests</name>
  <files>backend/app/api/resolutions.py, backend/app/main.py, backend/tests/test_resolutions.py</files>
  <action>
1. Create `backend/app/api/resolutions.py` with an `APIRouter()`. Follow the exact pattern from `api/decisions.py` for imports, auth dependencies, and error handling.

Endpoints:
- `GET ""` (list_resolutions): Query Decision where type="resolution", deleted_at=None, archived_at=None. Optional `status` filter. For each resolution, count signatures and total board-level members (role in admin/chair/board). Return paginated response with signature_count and total_signers per item. Also include resolution_number and document_id fields. Auto-generate resolution_number if missing using format `YYYY-NNN` (year of created_at + sequential count of resolutions in that year). NOTE: route path is `""` not `"/"` (matches redirect_slashes=False convention).

- `GET "/{resolution_id}"` (get_resolution): Return single resolution (Decision with type=resolution) with full detail including description, votes/results, and signature data. Re-use the same detail pattern from decisions.py get_decision endpoint.

- `POST "/{resolution_id}/sign"` (sign_resolution): Validate decision exists, type=="resolution", status=="closed". Check UniqueConstraint (no double-signing). Capture IP from X-Forwarded-For header first, fallback to request.client.host. Generate signature_hash via generate_signature_hash(). Create ResolutionSignature record. Add AuditLog entry with entity_type="resolution_signature". Return {status: "signed", signature_id, signed_at}.

- `GET "/{resolution_id}/signatures"` (get_signature_status): Return all board-level members (role in admin/chair/board, not deleted) with their signature status (signed_at or null). Return resolution_id, signatures list, signed_count, total_members.

2. In `backend/app/main.py`:
   - Add `resolutions` to the imports from `app.api`
   - Add `app.include_router(resolutions.router, prefix="/api/resolutions", tags=["resolutions"])` AFTER the decisions router line

3. Create `backend/tests/test_resolutions.py` with pytest integration tests using the existing conftest fixtures (db_session, client, seed_user). Tests needed:
   - `test_list_resolutions_only`: Create a vote-type and resolution-type decision, GET /api/resolutions returns only the resolution
   - `test_sign_resolution`: Create a closed resolution, POST sign, verify 200 with signature_id and signed_at
   - `test_sign_non_resolution_rejected`: Create a vote-type decision (closed), POST sign, verify 400
   - `test_sign_not_closed_rejected`: Create an open resolution, POST sign, verify 400
   - `test_duplicate_signature_rejected`: Sign once (200), sign again (400)
   - `test_signature_status`: Create closed resolution, sign with one user, GET signatures, verify signed_count=1 and member list includes signed/unsigned
   - `test_resolution_tools_registered`: Import TOOL_REGISTRY from app.tools, verify "create_resolution" and "list_resolutions" are registered (this test will pass after Plan 02, mark as xfail for now with reason="Agent tools registered in Plan 02")

All tests use `@pytest.mark.asyncio` and the `client` fixture. Create test decisions directly in db_session (not via API) for isolation.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/ivy/tmg-board/backend && python -m pytest tests/test_resolutions.py -x -q --ignore-glob="*test_resolution_tools*" 2>&1 | tail -20</automated>
  </verify>
  <done>All resolution API tests pass (except xfail tool registration). GET /api/resolutions returns only resolutions. Sign endpoint records name+timestamp+IP. Signatures endpoint shows who signed and who has not.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -v` passes (all existing + new tests)
- `GET /api/resolutions` returns only type=resolution decisions
- `POST /api/resolutions/{id}/sign` records signature with IP
- `GET /api/resolutions/{id}/signatures` returns all members with status
- Signing non-resolution or non-closed resolution returns 400
- Double-signing returns 400
</verification>

<success_criteria>
Resolution API fully operational with list, get, sign, and signatures endpoints. All integration tests pass. ResolutionSignature model and migration created.
</success_criteria>

<output>
After completion, create `.planning/phases/04-board-resolutions-resolution-writer/04-01-SUMMARY.md`
</output>
