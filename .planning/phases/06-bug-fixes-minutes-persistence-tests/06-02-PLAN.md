---
phase: 06-bug-fixes-minutes-persistence-tests
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_meetings_api.py
  - backend/tests/test_minutes_persistence.py
  - backend/tests/test_agent_api.py
  - backend/tests/test_auth.py
  - backend/tests/test_tool_handlers.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "All tests pass when run with pytest"
    - "Meetings CRUD endpoints have test coverage (list, get, create, update, cancel, start, end)"
    - "Agenda item CRUD endpoints have test coverage (add, reorder, update, delete)"
    - "Minutes persistence endpoints have test coverage (create, get, upsert, 404)"
    - "Auth dependency functions are tested with all 4 role levels"
    - "Agent API key endpoints have test coverage (get, put, masking)"
    - "Transcript tool handlers have test coverage"
  artifacts:
    - path: "backend/tests/test_meetings_api.py"
      provides: "Tests for 14+ meeting endpoints"
      min_lines: 200
    - path: "backend/tests/test_minutes_persistence.py"
      provides: "Tests for minutes create/get/upsert endpoints"
      min_lines: 80
    - path: "backend/tests/test_auth.py"
      provides: "Tests for auth dependency functions across all roles"
      min_lines: 60
  key_links:
    - from: "backend/tests/test_meetings_api.py"
      to: "backend/app/api/meetings.py"
      via: "httpx async test client"
      pattern: "/api/meetings"
    - from: "backend/tests/test_minutes_persistence.py"
      to: "backend/app/api/meetings.py"
      via: "POST/GET /api/meetings/{id}/minutes"
      pattern: "minutes"
---

<objective>
Add comprehensive test coverage for the most critical gaps: meetings CRUD (14 untested endpoints), minutes persistence (new from Plan 01), auth dependencies, agent API key endpoints, and transcript tool handlers.

Purpose: Bring test coverage from ~35% to ~60%+ by targeting the highest-impact untested areas.
Output: 5+ new test files with 80+ new test functions covering core domain logic.
</objective>

<execution_context>
@/Users/amirhaque/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amirhaque/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-bug-fixes-minutes-persistence-tests/06-CONTEXT.md
@.planning/phases/06-bug-fixes-minutes-persistence-tests/06-01-PLAN.md

<interfaces>
From backend/tests/conftest.py (existing fixtures):
```python
# Fixtures: db_engine (in-memory SQLite), db_session, seeded_db_session,
# client (httpx AsyncClient), seed_agent, seed_user (admin), seed_template, mock_litellm
```

From backend/app/api/meetings.py (endpoints to test):
```
GET  /api/meetings                          (require_member)
GET  /api/meetings/{id}                     (require_member)
POST /api/meetings                          (require_chair)
PATCH /api/meetings/{id}                    (require_chair)
DELETE /api/meetings/{id}                   (require_chair)
POST /api/meetings/{id}/start              (require_chair)
POST /api/meetings/{id}/end                (require_chair)
GET  /api/meetings/{id}/agenda             (require_member)
POST /api/meetings/{id}/agenda             (require_chair)
PATCH /api/meetings/{id}/agenda/reorder    (require_chair)
PATCH /api/meetings/{id}/agenda/{item_id}  (require_chair)
DELETE /api/meetings/{id}/agenda/{item_id} (require_chair)
GET  /api/meetings/{id}/attendance         (require_member)
POST /api/meetings/{id}/attendance         (require_chair)
GET  /api/meetings/members                 (require_member)
POST /api/meetings/with-agenda             (require_chair)
POST /api/meetings/{id}/minutes            (require_chair) — NEW from Plan 01
GET  /api/meetings/{id}/minutes            (require_member) — NEW from Plan 01
```

From backend/app/api/auth.py (require_* functions):
```python
def require_member(email: str = Header(..., alias="X-User-Email"), db: Session = Depends(get_db)) -> BoardMember:
    # Returns board member or 401
def require_chair(email: str = Header(..., alias="X-User-Email"), db: Session = Depends(get_db)) -> BoardMember:
    # Returns chair/admin or 401 (currently allows board/chair/admin)
def require_admin(email: str = Header(..., alias="X-User-Email"), db: Session = Depends(get_db)) -> BoardMember:
    # Returns admin only or 401
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test environment and add conftest fixtures</name>
  <files>
    backend/tests/conftest.py
  </files>
  <action>
1. **Ensure test dependencies are available:**
   ```bash
   cd backend && pip install pytest pytest-asyncio httpx litellm 2>&1 | tail -5
   ```
   Note: Use the existing venv if present, otherwise install in the project's Python environment.

2. **Add missing fixtures to `backend/tests/conftest.py`:**

   Add fixtures for:
   - `seed_meeting` — creates a Meeting (status="scheduled") linked to seed_user
   - `completed_meeting` — creates a Meeting (status="completed") for transcript/minutes tests
   - `seed_shareholder` — creates a BoardMember with role="shareholder" for permission testing
   - `seed_board_member` — creates a BoardMember with role="board" for permission testing
   - `seed_agenda_item` — creates an AgendaItem linked to seed_meeting

   Each fixture should use the `db_session` fixture and create/commit the objects.

3. **Fix the duplicate fixtures issue:**
   - `test_models.py` redefines `db_engine` and `db_session` — remove the duplicates and use conftest's fixtures
   - `mock_litellm` fixture in conftest is unused — keep it but add a comment noting it's available for future tests
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/drew/tmg-board/backend && python -m pytest tests/conftest.py --collect-only 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Test environment has all dependencies
    - New fixtures for meetings, roles, agenda items
    - Duplicate fixtures removed from test_models.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Add meetings CRUD and agenda tests</name>
  <files>
    backend/tests/test_meetings_api.py
  </files>
  <action>
Create `backend/tests/test_meetings_api.py` with tests covering:

**Meeting CRUD (minimum tests):**
- `test_list_meetings` — returns meetings list
- `test_list_meetings_filter_status` — filter by status query param
- `test_get_meeting_detail` — returns full meeting with agenda
- `test_get_meeting_not_found` — 404 for nonexistent ID
- `test_create_meeting` — POST creates meeting, returns ID
- `test_create_meeting_requires_chair` — 401 for shareholder
- `test_update_meeting` — PATCH updates fields
- `test_cancel_meeting` — DELETE sets status to cancelled
- `test_start_meeting` — POST transitions scheduled→in_progress
- `test_end_meeting` — POST transitions in_progress→completed

**Agenda Item CRUD (minimum tests):**
- `test_get_agenda` — returns agenda items ordered
- `test_add_agenda_item` — POST adds item to meeting
- `test_update_agenda_item` — PATCH updates item fields
- `test_delete_agenda_item` — DELETE removes item
- `test_reorder_agenda` — PATCH reorders items

**Attendance (minimum tests):**
- `test_get_attendance` — returns attendance list
- `test_record_attendance` — POST records batch attendance

**Members:**
- `test_list_members` — returns board members

Use the `client` and `seed_user` fixtures from conftest. Create meetings within each test as needed. Follow the existing test patterns in `test_transcripts.py` for async test setup.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/drew/tmg-board/backend && python -m pytest tests/test_meetings_api.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 18+ tests covering meetings CRUD, agenda, attendance, members
    - All tests pass
    - Auth checks verified (chair-only, member-only)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add minutes persistence, auth, and agent API key tests</name>
  <files>
    backend/tests/test_minutes_persistence.py,
    backend/tests/test_auth.py,
    backend/tests/test_agent_api.py
  </files>
  <action>
1. **Create `backend/tests/test_minutes_persistence.py`:**

   Tests for the new minutes endpoints (from Plan 01):
   - `test_create_minutes` — POST creates document + link
   - `test_create_minutes_requires_completed_meeting` — 400 for non-completed
   - `test_create_minutes_requires_chair` — 401 for shareholder
   - `test_get_minutes` — GET returns minutes content
   - `test_get_minutes_not_found` — 404 when no minutes
   - `test_upsert_minutes` — POST again updates existing (not duplicate)
   - `test_get_minutes_any_member` — shareholder can read minutes

2. **Create `backend/tests/test_auth.py`:**

   Tests for auth dependency functions:
   - `test_require_member_allows_admin` — admin passes require_member
   - `test_require_member_allows_board` — board passes
   - `test_require_member_allows_chair` — chair passes
   - `test_require_member_allows_shareholder` — shareholder passes
   - `test_require_member_rejects_unknown` — unknown email → 401
   - `test_require_chair_allows_admin` — admin passes require_chair
   - `test_require_chair_allows_board` — board passes (current behavior)
   - `test_require_chair_allows_chair` — chair passes
   - `test_require_chair_rejects_shareholder` — shareholder → 401
   - `test_require_admin_allows_admin` — admin passes
   - `test_require_admin_rejects_board` — board → 401
   - `test_require_admin_rejects_chair` — chair → 401
   - `test_no_header_returns_401` — missing X-User-Email → 401/422

   Test these via actual API calls through the `client` fixture, using endpoints that require each auth level.

3. **Add agent API key tests to `backend/tests/test_agent_api.py`:**

   Append tests:
   - `test_get_api_keys_admin` — GET /api/agents/api-keys returns masked keys
   - `test_get_api_keys_non_admin_403` — non-admin gets 403
   - `test_put_api_keys_admin` — PUT updates keys in settings table
   - `test_put_api_keys_non_admin_403` — non-admin gets 403
   - `test_api_keys_route_not_shadowed` — GET /api/agents/api-keys does NOT match /{slug}
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/drew/tmg-board/backend && python -m pytest tests/test_minutes_persistence.py tests/test_auth.py tests/test_agent_api.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 7+ minutes persistence tests all pass
    - 13+ auth dependency tests all pass
    - 5+ agent API key tests all pass (including route conflict verification)
  </done>
</task>

<task type="auto">
  <name>Task 4: Add transcript tool handler tests and run full suite</name>
  <files>
    backend/tests/test_tool_handlers.py
  </files>
  <action>
1. **Create `backend/tests/test_tool_handlers.py`:**

   Tests for the transcript/minutes tool handlers in `tools/transcripts.py`:
   - `test_get_meeting_details_success` — mocks httpx, verifies parallel fetch of meeting+agenda+attendance
   - `test_get_meeting_details_meeting_not_found` — 404 response handling
   - `test_get_meeting_transcript_success` — returns transcript content
   - `test_get_meeting_transcript_not_found` — handles 404
   - `test_create_minutes_document_success` — POST to /minutes endpoint succeeds
   - `test_create_minutes_document_fallback` — 404 returns content in fallback response

   Use `unittest.mock.patch` on `httpx.AsyncClient` to mock the HTTP calls. Follow the pattern from existing `test_tools.py` for httpx mocking.

2. **Run the full test suite:**
   ```bash
   cd backend && python -m pytest tests/ -v --tb=short 2>&1
   ```

   All existing + new tests should pass. Report the final count.
  </action>
  <verify>
    <automated>cd /Users/amirhaque/Files/swarmify/agents/drew/tmg-board/backend && python -m pytest tests/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 6+ transcript tool handler tests pass
    - Full test suite runs green
    - Total test count reported (target: 120+ total)
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -v` — all tests pass
- `wc -l backend/tests/test_meetings_api.py` — 200+ lines
- `wc -l backend/tests/test_minutes_persistence.py` — 80+ lines
- `grep -c "def test_" backend/tests/test_*.py` — 120+ total test functions
</verification>

<success_criteria>
- All tests pass (0 failures)
- Meetings CRUD has endpoint-level test coverage
- Minutes persistence endpoints are tested
- Auth functions are tested across all role levels
- Agent API key endpoints are tested
- Tool handlers are tested with mocked HTTP calls
- Total test count exceeds 120
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes-minutes-persistence-tests/06-02-SUMMARY.md`
</output>
